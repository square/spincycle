---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        type: job-type-a
        args: # arg-a expected twice
          - expected: arg-a
            given: arg-x
          - expected: arg-a
        deps: []
