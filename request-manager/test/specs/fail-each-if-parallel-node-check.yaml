---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        type: job-type-a
        args:
          - expected: required-a
        deps: []
        parallel: 3 # this shouldn't be specified without an each
