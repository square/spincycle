---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    acl:
      - role: eng
        ops: ["start"]
      - role: eng
        ops: ["ps"] # roles can only be defined once
    nodes:
      node-a:
        category: job
        type: job-type-a
        deps: []
