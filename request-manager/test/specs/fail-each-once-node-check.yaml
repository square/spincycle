---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        type: job-type-a
        each:
          - arg-a:alias-a # can't alias both two the same thing
          - arg-b:alias-a
        deps: []
