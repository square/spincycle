---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    acl:
      - role: eng
        admin: true # admin=true and ops are mutually exclusive
        ops: ["start"]
    nodes:
      node-a:
        category: job
        type: job-type-a
        deps: []
