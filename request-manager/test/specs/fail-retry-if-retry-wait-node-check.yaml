---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        type: job-type-a
        args:
          - expected: required-a
        deps: []
        retry: 0 # should be nonzero, since retryWait is specified
        retryWait: 500ms
