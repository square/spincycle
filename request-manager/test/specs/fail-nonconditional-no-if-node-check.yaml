---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        type: job-type-a
        if: nonexistent-arg # nonconditional nodes can't specify if
        args:
          - expected: arg-a
        deps: []
