---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        type: job-type-a
        each:
          - req-a:required-a
        args:
          - expected: required-a
        deps: []
        parallel: 0 # should be nonzero
