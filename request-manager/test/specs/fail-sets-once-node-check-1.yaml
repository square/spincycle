---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        type: job-type-a
        sets: # arg-a set twice
          - arg: arg-a
          - arg: arg-a
        deps: []
