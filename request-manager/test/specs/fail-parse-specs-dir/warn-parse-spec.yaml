---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        category: job # duplicated field--should generate warning
        type: job-type-a
        args:
          - expected: required-a
        deps: []
