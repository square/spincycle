---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        type: job-type-a
        args: # arg-a is used as input arg-a and as input arg-b
          - expected: arg-b
            given: arg-a
          - expected: arg-a
        deps: []
