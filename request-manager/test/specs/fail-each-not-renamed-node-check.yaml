---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        type: job-type-a
        each:
          - arg-a:alias-a # can't alias arg-a twice
          - arg-a:alias-b
        deps: []
