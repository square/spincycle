---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: conditional # conditionals must have `if`
        eq:
          yes: noop
          default: noop
        args:
          - expected: arg-a
        deps: []
