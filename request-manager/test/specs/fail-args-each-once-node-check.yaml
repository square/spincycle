---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        type: job-type-a
        each:
          - arg-a:alias-a
        args:
          - expected: alias-a # can't have arg-a and arg-b be renamed as alias-a
            given: arg-b
        deps: []
