---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: conditional # conditionals must have `if`
        if: nonexistent-arg
        args:
          - expected: arg-a
        deps: []
