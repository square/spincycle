---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        type: job-type-a
        sets:
          - arg: arg-a
          - as: arg-b # this should specify `arg`
        deps: []
