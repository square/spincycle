---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
    nodes:
      node-a:
        category: job
        type: job-type-a
        eq: # nonconditional nodes can't specify eq
          yes: noop
          default: noop
        args:
          - expected: arg-a
        deps: []
