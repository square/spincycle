sequences:
  req:
    args:
      required:
        - name: cmd
          desc: Shell command name
      static:
        - name: args # this should fail because default isn't specified
    nodes:
      job1name:
        category: job
        type: job1type
        args:
          - expected: cmd
            given: cmd
          - expected: args
            given: args
        sets: []
        deps: []
