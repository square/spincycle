---
sequences:
  seq-a:
    args:
      required:
        - name: required-a
      optional:
        - name: optional-a
          default: value-a
        - name: optional-b # this should have a default
    nodes:
      node-a:
        category: job
        type: job-type-a
        deps: []
