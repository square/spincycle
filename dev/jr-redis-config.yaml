---
# DO NOT EDIT! This is a template. It is copied into BUILD_DIR (dev/build) if
# if does not exist. To customize, edit BUILD_DIR/jr-redis-config.yaml. To regenerate
# from this template, rm BUILD_DIR/jr-redis-config.yaml and re-run the sandbox.
server:
  listen_address: "127.0.0.1:9999"
rm_client:
  server_url: "http://127.0.0.1:8888"
chain_repo_type: redis
redis:
  address: 127.0.0.1:6379
  idle_timeout: 250
  max_idle: 3
  network: tcp
  prefix: 'SpinCycle::ChainRepo'
