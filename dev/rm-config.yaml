---
# DO NOT EDIT! This is a template. It is copied into BUILD_DIR (dev/build) if
# if does not exist. To customize, edit BUILD_DIR/rm-config.yaml. To regenerate
# from this template, rm BUILD_DIR/rm-config.yaml and re-run the sandbox.
server:
  listen_address: "127.0.0.1:8888"
db:
  type: mysql
  dsn: "root:@tcp(127.0.0.1:3306)/spincycle_dev"
jr_client:
  server_url: "http://127.0.0.1:9999"
spec_file_dir: "../specs/"
